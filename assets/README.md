# README image assets

- `task3_pipeline.svg`: v4.5 AssemblyTransformer 추론 계약을 나타내는 결정적 벡터 도식

도식의 모델 크기, epoch와 GC 지표는 `RELEASE_MANIFEST.json` 및 보존된 GC snapshot에서 가져왔다.
실제 환자 mesh나 생성형 해부 이미지는 포함하지 않는다.
